- [ ] starlette TestClient emits deprecation warning suggesting httpx2; httpx2 does not exist as a real package yet (as of 2026-06), monitor starlette release notes and migrate when a stable httpx2 package is available
- [ ] frr-reload.py fails with rc=1 when protocols section is empty (all protocols deleted) - frr.conf has old config, NOS generates empty config, frr-reload.py cannot diff correctly
- [ ] SONiC integration: nos-cli as management plane for SONiC whitebox switches - new nos/drivers/sonic/ driver that writes to Redis CONFIG_DB instead of kernel/FRR
- [ ] DNS/nameservers: add `set system name-server <ip>` to config schema; apply to systemd-resolved via `resolvectl dns <irb-interface> <ip1> <ip2>` on commit. Currently NOS does not manage DNS at all - management IRB interface has no DNS until manually fixed with `resolvectl dns irb.<vlan> ...`
- [ ] port_vlan_map cleanup: when an interface is removed from NOS config (`delete interfaces vnetX ...` + commit, or interface disappears from kernel e.g. VM destroyed), remove its entry from the XDP `port_vlan_map` BPF map via PFE IPC (`xdp_loader_port_vlan_del`). Currently stale entries accumulate indefinitely (harmless but untidy)
- [ ] XDP attach fails on vnetX tap interfaces with 'File exists' on all three tiers (link-based native, legacy DRV_MODE, generic SKB_MODE); `show forwarding` reports vnetX as 'kernel' mode instead of xdp-generic. vnetX taps have an unusual `tc qdisc noqueue 8007:` already present (refcnt 2) - possibly a leftover from earlier manual XDP/port_vlan_map experiments, or something libvirt/qemu sets on tap creation. Functionally vnetX works correctly via kernel fallback (no packet loss), just without XDP fastpath. Needs investigation into where this qdisc comes from and whether nos-pfe should clear it before attempting XDP attach.
- [ ] VM recreate with a reused cloud-init seed ISO: cloud-init's `/etc/netplan/50-cloud-init.yaml` (generated on first boot, with `match: macaddress:` pinned to the original MAC) conflicts with `/etc/netplan/99-nos.yaml` after the VM is destroyed/undefined and recreated with a new MAC (same seed ISO reused, same instance-id so cloud-init skips re-running network config). `netplan apply` then fails with 'Cannot find unique matching interface for enp1s0'. Manual fix: `sudo rm /etc/netplan/50-cloud-init.yaml && sudo netplan apply` inside the VM. Long-term fix: regenerate the seed ISO with a new instance-id when recreating a VM, or document this manual step.
- [ ] `nos-cli` diagnostic command to clear stale/permanent bridge FDB entries on `nos-br` (e.g. `clear ethernet-switching table` or similar), useful when VM tap interfaces are recreated with new MACs
- [ ] cos-install.sh (agent role, cos repo) needs to set permissions on `/var/lib/cos` (o+x), `/var/lib/cos/images` (o+x, image files o+r), and `/var/lib/cos/vms` (o+x) so libvirt-qemu (uid 64055/gid kvm 994) can read template images and VM disks. Currently requires manual chmod after install - blocks COS<->NOS VM provisioning end-to-end on fresh installs. This is a cos repo fix, tracked here for visibility.
- [ ] General note: running `nos-cli` directly as a regular user (e.g. `super`) outside the `nos-api` service context can leave files in `/opt/nos/config/` with incorrect ownership, causing later `nos-api` commits to fail. The rollback rotation is now robust to this (709fa1a), but other config file writes during commit may have the same class of issue — worth a broader audit if similar permission errors recur.
