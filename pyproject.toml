[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cos"
version = "0.1.0"
description = "COS - Cloud Operating System"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["controller*", "agent*", "common*"]
